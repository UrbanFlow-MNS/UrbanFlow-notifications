services:
  notifications-db:
    image: postgres:15
    env_file:
      - .env.docker
    ports:
      - "5433:5432"
    volumes:
      - notifications-db:/var/lib/postgresql/data

volumes:
    notifications-db:
